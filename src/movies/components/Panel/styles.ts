import { styled } from '@mui/material/styles'
import Typography from '@mui/material/Typography'
import Paper from '@mui/material/Paper'

export const PanelWrapper = styled(Paper)({
  padding: 16,
  width: '100%',
})
export const PanelTitle = styled(Typography)({
  marginBottom: 16,
  fontWeight: 'bold',
})
